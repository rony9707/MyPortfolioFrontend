export interface skills {
  html: skillData;
  css: skillData;
  angular: skillData;
  powershell: skillData;
  MSSQL: skillData;
  RestApi: skillData;
  node: skillData;
  python: skillData;
}

export interface skillData {
  description: string;
  percentage: number;
}
