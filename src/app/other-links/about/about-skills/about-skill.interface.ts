export interface skills {
  html: skillData;
  css: skillData;
  angular: skillData;
  excel: skillData;
  MySQL: skillData;
  Postman: skillData;
  node: skillData;
  python: skillData;
}

export interface skillData {
  description: string;
  percentage: number;
}
